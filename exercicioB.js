
let a= parseInt(prompt("Digite o primeiro valor"))
let b= parseInt(prompt("Digite o Segundo valor"))
let c = parseInt(prompt("Digite o terceiro valor"))
let maior

     if( a > b)
      maior = a;
     else 
      maior =b 
     if( a > c)
      maior = a;
     else
      maior =c
     if (b > c )
      maior = b
     else 
      maior=c

  
alert("O Maior número digtado foi " + maior)